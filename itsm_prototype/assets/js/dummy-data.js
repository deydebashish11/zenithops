/* ============================================================
   ZENITHOPS – DUMMY DATA GENERATOR
============================================================ */

function generateDummyData() {
    const summaries = [
        "VPN not connecting", "Email outage", "Database latency",
        "Server down", "App crash", "User cannot login",
        "Network slowness", "Printer offline", "API failure",
        "Security alert triggered", "Backup failed", "Certificate expired"
    ];

    const callers = ["John", "Amit", "Sara", "Priya", "Mark", "Li Wei", "David", "Nisha"];

    const priorities = ["P1", "P2", "P3", "P4"];
    const states = ["New", "In Progress", "On Hold", "Resolved"];

    const result = [];

    for (let i = 1; i <= 25; i++) {
        result.push({
            id: "INC-" + String(i).padStart(5, "0"),
            summary: summaries[Math.floor(Math.random() * summaries.length)],
            description: "Autogenerated sample incident for demo.",
            caller: callers[Math.floor(Math.random() * callers.length)],
            priority: priorities[Math.floor(Math.random() * priorities.length)],
            state: states[Math.floor(Math.random() * states.length)],
            opened: new Date(Date.now() - Math.random() * 86400000).toLocaleString(),
            updated: new Date().toLocaleString(),
            sla_seconds: Math.floor(Math.random() * 20000),
            business: "IT Services",
            location: "Bangalore",
            category: "Infrastructure",
            subcategory: "Network",
            ci: "CISCO Router",
            group: "Network Support",
            assigned_to: "Automation Engine",
            work_notes: [],
            timeline: []
        });
    }

    localStorage.setItem("incidents_db", JSON.stringify(result));
    alert("25 Dummy Incidents Generated!");

    window.location.reload();
}
